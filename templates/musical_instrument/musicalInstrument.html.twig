<div class="row align-items-center">
	<div class="col">
	    <h3>Muziekinstrument</h3>
    </div>
        
    <div class="col-4  ">
	    <div class="btn-group float-right">
	    	<a href="{{ path('app_person_musicalInstrument_create', {'id': data.id}) }}" class="btn btn-sm btn-trans btn-purple waves-effect"><i class="fa fa-plus"></i></a>
	    </div>
    </div>
        
</div>
{%if data.musicalInstrument is not empty %}
<table class="table table-bordered table-striped">   
    <tbody>
        <thead>
            <tr>
            	<td><b>Muzieksinstrument</b></td>
        	</tr>
        </thead>
        
        <tr>
            <td>
	            
	            <!-- Dropdown -->
			    <div class="dropdown pull-right">
                    <a href="" class="dropdown-toggle arrow-none card-drop" data-toggle="dropdown" aria-expanded="false">
                        <i class="mdi mdi-dots-horizontal"></i>
                    </a>
                    
                    <div class="dropdown-menu dropdown-menu-right">
                        <!-- item-->
                        <a href="{{ path('app_person_musicalInstrument_edit', {'id': data.id}) }}" class="dropdown-item">Bewerken</a>
                        <!-- item-->
                        <a href="{{ path('app_person_musicalInstrument_delete', {'id': data.id}) }}" class="dropdown-item">Verwijderen</a>
                    </div>
                </div>
	            
	            {{data.musicalInstrument.musicalInstrument}}
            </td>
        </tr>
   
    </tbody>
</table>
{% endif %}