<div class="row align-items-center">
	<div class="col">
	    <h3>Medewerkers</h3>
    </div>
        
    <div class="col-4  ">
	    <div class="btn-group float-right">
	    	{% if is_granted('ROLE_ORGANIZATION_CREATE') %}
	    	<a href="{{ path('app_organization_employee_edit', {'id': data.id}) }}" class="btn btn-sm btn-trans btn-purple waves-effect"><i class="fa fa-plus"></i></a>
	    	{% endif %}
	    </div>
    </div>
        
</div>
{%if data.employee is not empty %}
<table class="table table-bordered table-striped">   
    <tbody>
        <thead>
            <tr>
            	<td><b>Naam</b></td>
            	<td><b>Email</b></td>
            	<td><b>Categorie</b></td>
        	</tr>
        </thead>
    {% for employee in data.employee %}
        <tr>
            <td>
            	<a href="{{ path('app_person', {'id': employee.id}) }}">{{ employee.familyName }}, {{ employee.givenName }} {{ employee.additionalName }}</a>
            	{% if is_granted('ROLE_ORGANIZATION_EDIT') %}
	            <!-- Dropdown -->
			    <div class="dropdown pull-right">
                    <a href="" class="dropdown-toggle arrow-none card-drop" data-toggle="dropdown" aria-expanded="false">
                        <i class="mdi mdi-dots-horizontal"></i>
                    </a>
                    
                    <div class="dropdown-menu dropdown-menu-right">
                        <!-- item-->
                        <a href="{{ path('app_organization_employee_edit', {'id': data.id}) }}" class="dropdown-item">Bewerken</a>
                    </div>
                </div>
                {% endif %}
            </td>
            <td>
	            <a href="mailto:{{ employee.email }}">{{ employee.email }}</a>
            </td>
            <td>
	         	{{ employee.category.name }}	
            </td>
        </tr>
    {% endfor %} 
   
    </tbody>
</table>
{% endif %} 